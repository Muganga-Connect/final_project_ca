<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/splash_background">

    <!-- Logo -->
    <ImageView
        android:id="@+id/splash_logo"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/splash_title"
        android:layout_marginBottom="24dp"
        android:src="@drawable/ic_splash_logo" />

    <!-- Main Title -->
    <TextView
        android:id="@+id/splash_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/splash_subtitle"
        android:layout_marginBottom="8dp"
        android:text="MugangaConnect"
        android:textColor="@color/splash_text_blue"
        android:textSize="28sp"
        android:textStyle="bold" />

    <!-- Orange Plus Sign -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/splash_title"
        android:layout_alignTop="@+id/splash_title"
        android:layout_marginLeft="4dp"
        android:text="+"
        android:textColor="@color/splash_accent_orange"
        android:textSize="24sp"
        android:textStyle="bold" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/splash_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="GLOBAL HEALTHCARE CONNECTIONS"
        android:textColor="@color/text_dark"
        android:textSize="14sp"
        android:fontFamily="sans-serif"
        android:letterSpacing="0.1" />

</RelativeLayout>